# Node.js Checkpoint Project

This repository contains a series of beginner-friendly exercises designed to introduce Node.js concepts, including working with the console, creating servers, manipulating the file system, using external packages (like `generate-password`), and sending emails with `nodemailer`.

---

## 📁 Project Structure

Each exercise is contained in its own file:

